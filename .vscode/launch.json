{
    "version": "0.2.0",
    "configurations": [
        
        {
            "name": "Rodar Menu Olímpico",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/programa_final.exe",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "externalConsole": true,
            "MIMode": "gdb",
            "miDebuggerPath": "gdb",
            "setupCommands": [
                {
                    "description": "Habilitar a formatação perfeita para gdb",
                    "text": "-enable-pretty-printing",
                    "ignoreFailures": true
                }
            ],
            "preLaunchTask": "Compilar PROJETO COMPLETO"
        }
    ]
}
